notes: {}
server:
  command: npx
  args: ["-y", "clirank-mcp-server"]
tracks:
  - name: discover APIs
    tool: discover_apis
    args:
      category: weather
    expect:
      contains: api
  - name: browse categories
    tool: browse_categories
    args: {}
    expect:
      contains: Total
