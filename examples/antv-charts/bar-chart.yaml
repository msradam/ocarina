notes: {}

server:
  command: npx
  args: ["-y", "@antv/mcp-server-chart"]

tracks:
  - name: generate bar chart
    tool: generate_bar_chart
    args:
      data:
        - x: "Jan"
          y: 100
        - x: "Feb"
          y: 150
        - x: "Mar"
          y: 120
      xField: x
      yField: y
    expect:
      contains: http
