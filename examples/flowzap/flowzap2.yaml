notes: {}
server:
  command: npx
  args: ["-y", "flowzap-mcp"]
tracks:
  - name: validate syntax error
    tool: flowzap_validate
    args:
      code: "box A [User]"
    expect:
      contains: Validation
