keys:
  target: /tmp/eslint-test/test.js

server:
  command: npx
  args: ["-y", "@eslint/mcp"]

rondo:
  - name: lint JavaScript file
    tool: lint-files
    args:
      filePaths: ["{{target}}"]
    expect:
      contains: no-var
